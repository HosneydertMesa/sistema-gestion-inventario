spring.application.name=msProductos

# Configuraci�n de la Base de Datos para Pruebas
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=hosneydert92

# Especifica el driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraci�n de Hibernate (muy �til para pruebas)
# 'create-drop': crea el esquema al inicio de las pruebas y lo borra al final.
spring.jpa.hibernate.ddl-auto=create-drop

server.port=8092

security.api-key.secret=mi-api-key-secreta